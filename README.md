# snake-game

A tiny browser Snake game — single `index.html`, no dependencies.

## Play
Open `index.html` in any browser. Arrow keys to move, Space to restart.

## Why this repo exists
Demo/sandbox repo used to showcase Rubrik Security Cloud DevOps Protection (GitHub backup & recovery).
# snake
# snake
